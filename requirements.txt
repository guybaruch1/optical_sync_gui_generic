pyrealsense2==2.58.3.10794
opencv-python==5.0.0.93
numpy==2.5.1
PyYAML==6.0.3
PySide6==6.11.1
pyqtgraph==0.14.0
matplotlib==3.11.1
pytest==9.1.1
# Only needed for the optional dual-LED-panel setup (Device Select's "Use
# dual LED panel" checkbox) - engine/dual_panel_control.py imports both
# lazily, so every normal single-panel run works without either installed.
pyserial==3.5
# Acroname's USB-hub SDK - NOT necessarily a plain PyPI install; confirm
# the actual install method on the dev machine (Acroname sometimes
# distributes this via their own installer rather than PyPI).
brainstem
# Only needed for LEDPanel's --get*/--isRunning query commands
# (engine/led_panel.py's _query, imported lazily there) - LED-Panel.exe
# writes their answers via the low-level WriteConsole API, which produces
# nothing when stdout is redirected to a pipe/file (confirmed on real
# hardware: works fine printed directly to a terminal, empty when
# redirected with `>`), so capturing them needs to read the console
# screen buffer directly instead of normal subprocess output capture.
pywin32==308
